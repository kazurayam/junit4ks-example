/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn how to create Gradle builds at https://guides.gradle.org/creating-new-gradle-builds/
 */
plugins {
    id 'groovy'
    id "com.katalon.gradle-plugin" version "0.0.4"

    // https://github.com/michel-kraemer/gradle-download-task
    id "de.undercouch.download" version "3.4.3"
}


/*
repositories {
    ivy {
        url 'https://github.com/kazurayam'
	layout('pattern') {
	    // https://github.com/kazurayam/junit4ks/releases/download/1.6/junit4ks-all.jar
            artifact'[artifact]/releases/download/[revision]/[artifact]-all.jar'
        }
    }
}
dependencies {
    compile 'com.kazurayam:junit4ks:1.6'
}
 */

// $ gradle katalonCopyDependencies


import java.nio.file.Path

ext {
    dirDrivers = projectDir.toPath().resolve('Drivers').toFile()
}

task downloadFile(type: Download) {
    src 'https://github.com/kazurayam/junit4ks/releases/download/1.6/junit4ks-all.jar'
    dest dirDrivers
}